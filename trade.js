// Redirect users to stake.html when clicking the button
const stakeButton = document.getElementById("stakeButton");

stakeButton.addEventListener("click", () => {
  window.location.href = "stake.html"; // navigate to your stake page
});